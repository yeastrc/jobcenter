
Follow these steps to run the client.

  Unjar/unzip this jar file in a directory
  
  Follow the other README files for needed configuration.
  
  Install modules.
  
  Run the shell script to start the client.
  
  
  The client will respond to shutdown requests ( ctrl-c ) and pass those to the module for a hopefully clean shutdown.